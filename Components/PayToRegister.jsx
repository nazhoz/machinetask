import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PayToRegister = () => {
  return (
    <View>
      <Text>PayToRegister</Text>
    </View>
  )
}

export default PayToRegister

const styles = StyleSheet.create({})